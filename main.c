#include<stdio.h>
#include<stdlib.h>
#include<mem.h>

const int n=10;

char * memset(char *[], char ch, int n)

{

int i;
for (i=0;i<n;i++)
v[i] = ch;

return v;

}

main()
{
   char v[n], char chara = 'O'
   char * memset(v*[], chara, n);


    return 0;
}